<Window x:Class="Travelt.DiscoveryPageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Travelt"
        mc:Ignorable="d"
        Title="Discovery Feed" Height="600" Width="900">

    <Grid Background="#1A001F">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,30">

                <Border Width="675" 
                        Background="#F4F4F4" 
                        CornerRadius="25" 
                        Padding="5" 
                        Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="0"/>
                    </Border.Effect>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="SearchBox"
                                 KeyDown="SearchBox_KeyDown"
                                 Grid.Column="0"
                                 Padding="15,0" 
                                 FontSize="14" 
                                 Background="Transparent"
                                 VerticalContentAlignment="Center"
                                 BorderThickness="0" />

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <ComboBox x:Name="searchvalues"
                                      Width="90" 
                                      Background="Transparent"
                                      BorderThickness="0"
                                      BorderBrush="Transparent"
                                      FontWeight="Bold"
                                      FontSize="15"
                                      Foreground="Black"
                                      VerticalContentAlignment="Center"
                                      HorizontalContentAlignment="Center">
                                <ComboBoxItem Content="Posts" IsSelected="True"/>
                                <ComboBoxItem Content="People"/>
                                <ComboBoxItem Content="Trips"/>
                            </ComboBox>

                            <Button Content="Search"
                                    Click="search_click"
                                    Width="90"
                                    Height="40"
                                    Foreground="White" 
                                    FontWeight="Bold"
                                    FontSize="15"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#FF2D8C"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            CornerRadius="0,20,20,0">
                                                        <ContentPresenter HorizontalAlignment="Center" 
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#D41D72"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter Property="Background" Value="#1A001F"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <ItemsControl x:Name="DiscoverFeed">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:PostUI HorizontalAlignment="Center" Width="675"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
        <Label x:Name="noresultslabel"
       Content="No results found :("
       HorizontalAlignment="Center" 
       VerticalAlignment="Center" 
       FontSize="30" 
       Foreground="White" 
       FontWeight="Bold" 
       Visibility="Collapsed">
        </Label>
    </Grid>
</Window>