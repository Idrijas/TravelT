<Window x:Class="Travelt.AdminToolboxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Toolbox"
        Height="666"
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#1A001F">

    <Window.Resources>
        <Style x:Key="AdminButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="240"/>
            <Setter Property="Height" Value="90"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FF2D8C"/>
            <Setter Property="FontFamily" Value="Comic Sans MS"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20">

        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            
            <RowDefinition Height="*"/>
            
            <RowDefinition Height="Auto"/>
           
        </Grid.RowDefinitions>

        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            
            <ColumnDefinition Width="*"/>
            
            <ColumnDefinition Width="250"/>
            
        </Grid.ColumnDefinitions>

        
        <TextBlock Text="Admin Toolbox"
               Grid.Row="0"
               Grid.ColumnSpan="3"
               FontSize="40"
               FontFamily="Comic Sans MS"
               Foreground="White"
               HorizontalAlignment="Center"
               Margin="0,0,0,20"/>

        
        <StackPanel Grid.Row="1" Grid.Column="0">
            <TextBlock Text="User Management"
                   FontFamily="Comic Sans MS"
                   Foreground="White"
                   FontSize="22"
                   Margin="0,0,0,10"/>

            <Button Content="View Users" Style="{StaticResource AdminButtonStyle}" Click="ViewUsersButton"/>
            <Button Content="Edit Users" Style="{StaticResource AdminButtonStyle}" Click="EditUsersButton"/>
            <Button Content="View Users Reports" Style="{StaticResource AdminButtonStyle}" Click="ViewUsersReportsButton"/>
            <Button Content="Delete User" Style="{StaticResource AdminButtonStyle}" Click="DeleteUserButton"/>
        </StackPanel>

        
        <StackPanel Grid.Row="1" Grid.Column="2">
            <TextBlock Text="Trip Management"
                   FontFamily="Comic Sans MS"
                   Foreground="White"
                   FontSize="22"
                   Margin="0,0,0,10"/>

            <Button Content="View Trips" Style="{StaticResource AdminButtonStyle}" Click="ViewTripsButton"/>
            <Button Content="Edit Trips" Style="{StaticResource AdminButtonStyle}" Click="EditTripsButton"/>
            <Button Content="View Trips Reports" Style="{StaticResource AdminButtonStyle}" Click="ViewTripsReportsButton"/>
            <Button Content="Delete Trip" Style="{StaticResource AdminButtonStyle}" Click="DeleteTripButton"/>
        </StackPanel>


        <DataGrid x:Name="AdminDataGrid"
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="10"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="50"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>


        <Button Content="Exit"
            Grid.Row="2"
            Grid.Column="0"
            Width="120"
            FontFamily="Comic Sans MS"
            Background="#FF2D8C"
            Foreground="White"
            Height="40"
            HorizontalAlignment="Left"
            Margin="10"
            Click="ExitButton"/>
        
        <Button Content="Clear"
                Grid.Row="2"
                Grid.Column="2"
                Width="120"
                FontFamily="Comic Sans MS"
                Background="#FF2D8C"
                Foreground="White"
                Height="40"
                HorizontalAlignment="Left"
                Margin="10"
                Click="ClearButton"/>
                

    </Grid>

</Window>