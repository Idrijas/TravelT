<UserControl x:Class="Travelt.PostUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Travelt"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="400">

    <Border Background="White" 
            CornerRadius="10" 
            Margin="10" 
            BorderBrush="#E0E0E0" 
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="2" Direction="-90"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="12">
                <Ellipse Width="50" Height="50">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding Profile_PictureFullPath}" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>
                <StackPanel Margin="12,0,0,0">
                    <TextBlock Text="{Binding Username}" FontWeight="Bold" FontSize="18"/>
                    <TextBlock Text="{Binding GeneratedTimestamp}" Foreground="#FF2D8C" FontSize="10"/>
                </StackPanel>
            </StackPanel>

            <Image Grid.Row="1" 
                   Source="{Binding ImageFullPath}"
                   Stretch="UniformToFill" 
                   MaxHeight="450" 
                   MinHeight="300"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

            <StackPanel Grid.Row="2" Margin="12">
                <TextBlock Text="{Binding Description}" 
                           TextWrapping="Wrap" 
                           FontSize="14" 
                           LineHeight="18"
                           Margin="10,0,10,0"/>
            </StackPanel>

            <Border Grid.Row="3" BorderBrush="#F0F0F0" BorderThickness="0,1,0,0" Margin="8">
                <StackPanel Orientation="Horizontal">
                    <Button Click="Like_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="❤" Foreground="{Binding LikeIconColor}" FontSize="18" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding LikeCount}" Margin="5,0,20,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Click="CommentToggle_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💬" FontSize="18" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding CommentCount}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="4" Visibility="{Binding CommentsVisibility}" Margin="12,0,12,12">
                <Separator Background="#F0F0F0" Margin="0,0,0,10"/>
                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Comments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Width="25" Height="25" VerticalAlignment="Top">
                                        <Ellipse.Fill>
                                            <ImageBrush ImageSource="{Binding Profile_Picture}" Stretch="UniformToFill"/>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                        <TextBlock Text="{Binding Username}" FontWeight="Bold" FontSize="12"/>
                                        <TextBlock Text="{Binding Comment_Text}" TextWrapping="Wrap" FontSize="12"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                 <TextBox x:Name="CommentInput" 
                          KeyDown="CommentInput_KeyDown"
                          Padding="8" 
                          Background="#F4F4F4" 
                          BorderThickness="0" 
                          VerticalContentAlignment="Center"/>

                    <TextBlock IsHitTestVisible="False" 
                   Text="Write a comment..." 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Left" 
                   Margin="10,0,0,0" 
                   Foreground="DarkGray">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=CommentInput}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <Button Grid.Column="1" 
            Content="Send" 
            Click="AddComment_Click" 
            Margin="5,0,0,0" 
            Padding="15,5" 
            Background="#FF2D8C" 
            Foreground="White" 
            BorderThickness="0"
            Cursor="Hand"/>
        </Grid>
        </StackPanel>
        </Grid>
    </Border>
</UserControl>